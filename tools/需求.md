帮我写一个的FormatTools.py脚本

# 对content下面的所有md文件进行文件名检查和重命名

1.文件名全部小写
2.移除文件名中的中文字符
3.分隔符使用_代替-只使用小写字母：将 Animancer_Why.md 改为 animancer-why.md
4.检查文件里面，如果涉及到链接的，也要更新成新命名的markdown文件链接

# 如果content下面的所有发现.meta后缀的文件，请删除

# 如果发现子文件夹有大写字母，把当前文件夹重命名为小写字母

# 如果hugo.toml 有链接应为重命名 也帮我更新一下

# 每个markdown页面添加 Front Matter（前置元数据）通常位于 Markdown 文件的最顶部
参考如下格式
---
title: "Animancer IK 深度解析"           # 文章标题
date: 2025-12-25T10:00:00+08:00        # 发布日期（注意 ISO 8601 格式）
lastmod: 2025-12-25T12:00:00+08:00     # 最后修改日期
draft: false                           # 是否为草稿（必须设为 false 才能在正式站显示）
author: "mickorz"                      # 作者
tags: ["Unity", "Animancer", "IK"]      # 标签，用于分类索引
categories: ["技术笔记"]                # 分类
description: "本文详细介绍了如何在 Unity 中使用 Animancer 插件配置 IK 系统。" # 摘要
                             # 排序权重，数字越小越靠前
---

# 链接修改
检查每个markdown中的链接，改成下面这种格式
改成使用 Hugo 官方推荐的 ref（最稳妥）

不允许下面使用下面的[\1]
[\1]({{< ref "./path/$1.md" >}})
使用这种格式
[$1]({{< ref "./path/$1.md" >}})




